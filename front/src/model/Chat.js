class Chat {
    constructor(
        chatroomId,
        message,
        type,
    ) {
        this.chatroomId = chatroomId;
        this.message = message;
        this.type = type;
    }
}

export default Chat
